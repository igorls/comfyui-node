import { describe, it, expect } from 'bun:test';
// Deprecated progress-only spec: covered by call-wrapper-streaming.spec.ts now.
describe.skip('Deprecated call-wrapper-progress spec', () => {
  it('placeholder', () => expect(true).toBe(true));
});
