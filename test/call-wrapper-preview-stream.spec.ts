// Deprecated spec: superseded by call-wrapper-streaming.spec.ts
// Keeping as skipped placeholder to avoid re-introducing brittle mock lifecycle logic.
import { describe, it, expect } from 'bun:test';
describe.skip('Deprecated call-wrapper-preview-stream spec', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
